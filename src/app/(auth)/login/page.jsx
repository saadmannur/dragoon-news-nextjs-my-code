import React from 'react';

export const metadata = {
    title: "Dragoon News | LogIn",
};

const LoginPage = () => {
    return (
        <div>
            Login page
        </div>
    );
};

export default LoginPage;